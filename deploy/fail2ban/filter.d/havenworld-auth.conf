# /etc/fail2ban/filter.d/havenworld-auth.conf
# Bans IPs that repeatedly POST to /api/auth/login and receive a 401 or 429 response

[Definition]
failregex = ^<HOST> .* "POST /api/auth/login HTTP/\d\.\d" (401|429) .*$
ignoreregex =
